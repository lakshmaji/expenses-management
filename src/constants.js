const MAXIMUM_OCCUPANCY = 3;
const INITIAL_BALANCE = 0;
// To avoid geektrust magic number check, bit annoying. But this is what i endup with
const MINIMUM_MEMBERS_REQUIRED = FILENAME_POSITION = 2;

module.exports = {
  MAXIMUM_OCCUPANCY,
  INITIAL_BALANCE,
  MINIMUM_MEMBERS_REQUIRED,
  FILENAME_POSITION,
};
